const array=[10,20,30,40,50,60,70];
for(let ele in array){
    // console.log(`index is ${ele} and value is ${array[ele]}`);
};

// in array - 1. values of  2. index in

const obj={
    name:"abhishek",
    age:23,
    city:"ujjain",
    dob:"01-11-2000",
}


for(let x of Object.values(obj)){
    console.log(x);
}
// const objarr=Object.values(obj);
// console.log(objarr);
